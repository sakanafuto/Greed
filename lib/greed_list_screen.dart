import 'package:flutter/material.dart';

class GreedListScreen extends StatelessWidget {
  const GreedListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(
          child: Text("list"),
        )
      ),
    );
  }
}
